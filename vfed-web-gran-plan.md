# VFED Web Gran Plan — UI / WebApp Layer

> 所有UI布局、交互设计、前端渲染、用户体验的改动都在此文件。
> 仿真模型/计算方法改动见 `vfed-models-plan.md`。

---

## 0. Baseline — Existing Web App (`vfed-web/`)

| 组件 | 已有 | 问题 |
|------|------|------|
| 运行时 | Pyodide Web Worker, 浏览器内跑完整Python | ✅ 保留 |
| 配置输入 | YAML编辑器(textarea) | 对非技术用户不友好, 无结构化表单 |
| 参数扫描 | 参数范围编辑器, 笛卡尔积sweep | 单点评估和sweep混在一起 |
| 结果展示 | 散点图(Chart.js), 成本分解柱状图, 结果表 | 需要手动点击Run才更新, 无实时预览 |
| 预设 | 609 Fengxian, Lettuce Standard | ❌ 无地区/电价数据库 |
| URL | 静态, 无状态编码 | ❌ 无法分享当前参数 |
| 语言 | 仅英文 | ❌ 无多语言 |
| 用户系统 | 无 | ✅ 保持无注册 |

**保留的架构**: Pyodide Worker + bundler (`bundle.py`) → 所有Python代码在浏览器执行。

---

## 1. Core UI Architecture

```
┌──────────────────────────────────────────────────────────┐
│  顶部Bar                                                  │
│  [🌐 语言]  [📋 场景名称]  [🔗 复制分享链接]              │
├─────────────────────────┬────────────────────────────────┤
│                         │                                │
│  左面板: 设计参数       │  右面板: 效果实时预览           │
│  ≈40% 宽度             │  ≈60% 宽度                     │
│                         │                                │
│  ┌───────────────────┐  │  ┌──────────────────────────┐  │
│  │ ▼ 场景设定        │  │  │ 模式: [单方案] [参数遍历] │  │
│  │   地区/气候数据库  │  │  ├──────────────────────────┤  │
│  │   电价区域         │  │  │                          │  │
│  │   栽培面积/架参数  │  │  │  ┌─ 指标卡片 ──────────┐ │  │
│  │   产量+蒸腾设定    │  │  │  │ kWh/kg  年总MWh     │ │  │
│  ├───────────────────┤  │  │  │ 年产量   收获次数     │ │  │
│  │ ▼ 围护结构        │  │  │  └──────────────────────┘ │  │
│  │   传热系数UA       │  │  │                          │  │
│  │   窗户面积/得热率  │  │  │  ┌─ 每月能耗柱状图 ────┐ │  │
│  │   换气次数ACH      │  │  │  │ ▊▊▊▊▊▊▊▊▊▊▊▊      │ │  │
│  │   体积/热容        │  │  │  └──────────────────────┘ │  │
│  ├───────────────────┤  │  │                          │  │
│  │ ▼ LED             │  │  │  ┌─ 能耗组成饼图 ──────┐ │  │
│  │   PPFD / 光效     │  │  │  │ HVAC DEH LED 其它     │ │  │
│  │   光周期           │  │  │  └──────────────────────┘ │  │
│  │   覆盖面积         │  │  │                          │  │
│  ├───────────────────┤  │  │  ┌─ 跨季节分时曲线 ────┐ │  │
│  │ ▼ HVAC            │  │  │  │ 1月 ── 4月 ── 7月    │ │  │
│  │   COP设定          │  │  │  │ ── 10月             │ │  │
│  │   死区/最短启停    │  │  │  └──────────────────────┘ │  │
│  ├───────────────────┤  │  │                          │  │
│  │ ▼ 除湿机          │  │  │  ┌─ AI策略对比(W后续) ──┐ │  │
│  │   SMER / 死区     │  │  │  │ AI光 AI温 AI电 vs固定 │ │  │
│  ├───────────────────┤  │  │  └──────────────────────┘ │  │
│  │ ▼ PV + Battery    │  │  │                          │  │
│  │   面积/倾角/朝向  │  │  │  ┌─ 3D视图(W6+) ───────┐ │  │
│  │   电池容量/C-rate │  │  │  │ [Three.js canvas]     │ │  │
│  ├───────────────────┤  │  │  └──────────────────────┘ │  │
│  │ ▼ 经济参数        │  │  │                          │  │
│  │   设备单价         │  │  └──────────────────────────┘  │
│  │   利率/折旧        │  │                                │
│  └───────────────────┘  │                                │
│                         │                                │
└─────────────────────────┴────────────────────────────────┘
```

**核心交互规则**:
- 参数修改 → 500ms debounce → Pyodide Worker自动重新计算 → 右侧所有图表实时刷新
- 左侧面板折叠/展开, 默认全部展开
- 两个模式: "单方案评估"（默认）和 "参数遍历范围计算"
- 不设用户注册, 即开即用
- 首次访问弹语言选择, 后续在顶部Bar切换

---

## 2. Implementation Phases

### Phase W1 — 纯数值, 实时预览（最先实现）

**目标**: 左侧参数 → 改一个值 → 右侧所有图表自动更新, 无需手动点击Run。

**左侧参数面板**（结构化表单, 替代当前YAML textarea）:

| 分组 | 字段 | 类型 |
|------|------|------|
| 场景设定 | 地区(城市搜索), 栽培面积, 单位面积产量, 蒸腾模式, 种植周期 | 搜索框+数字+下拉 |
| 围护结构 | U_wall_A, A_window, η_solar, ACH, V_room, C_z | 数字+滑块 |
| LED | ppfd_target, efficacy, photoperiod_hours, heat_fraction | 数字+滑块 |
| HVAC | cop_value, deadband_c, 加热模式 | 数字+下拉 |
| 除湿机 | smer, deadband_rh | 数字+滑块 |
| PV | A_pv, tilt, azimuth, η_pv | 数字+滑块 |
| 电池 | E_bat, c_rate, soc_min/max | 数字+滑块 |
| 经济 | interest_rate, currency | 数字+下拉 |
| 电价 | 24小时逐时价格表 | 数字数组 |

**右侧实时预览**（每次参数变化自动重算）:

| 图表 | 类型 | 内容 |
|------|------|------|
| 指标卡片 | 数字卡片 | kWh/kg, 年总MWh, 年产量(kg), 收获次数 |
| 每月能耗 | 堆叠柱状图 | 12月, 每柱分HVAC/DEH/LED/Pump/Other |
| 能耗组成 | 环形图 | 年总量中各设备占比 |
| 跨季节分时 | 折线图 | 1月/4月/7月/10月典型日24h负荷曲线 |
| 室内气候 | 双轴折线图 | T_z全年, RH_z全年(可选显示) |

**技术方案**:
- 所有计算保持在Pyodide Worker内, 不做服务端
- 参数表单 ↔ 内部YAML(隐藏) ↔ Worker
- Worker返回JSON → 前端渲染Chart.js
- debounce 500ms防抖

**改动范围**:
- `vfed-web/index.html`: 重构为左侧表单+右侧预览布局
- `vfed-web/main.js`: 表单状态管理, 自动计算触发, 多图表渲染
- `vfed-web/worker.js`: 保持不变（已能跑完整仿真）

---

### Phase W2 — 统一URL分享

**目标**: 当前所有参数压缩到URL中, 分享即复现。

**方案**:
- 所有参数值序列化 → base64/gzip压缩 → 作为 `?state=xxx` 参数
- 打开带state的URL → 自动解析 → 填充表单 → 触发计算
- "复制分享链接"按钮 → 写入剪贴板 → toast提示

**改动范围**:
- `vfed-web/main.js`: 新增 `encodeState()` / `decodeState()`, URL监听

---

### Phase W4 — 参数遍历范围计算（单方案→优化模式切换）

**目标**: 右侧模式切换到"参数遍历范围计算"，可视化参数空间。

**右侧变化**（切换到遍历模式时）:

| 组件 | 内容 |
|------|------|
| 参数选择器 | 勾选要遍历的参数（PPFD, COP, PV面积, 电池容量...） |
| 范围设定 | 每个选中参数: min/max/step 三个输入框 |
| 散点图 | Chart.js scatter: X=参数A, Y=参数B, 颜色=kWh/kg(绿→红) |
| 最优方案卡 | 高亮显示: kWh/kg最低的配置, 各参数值 |
| 结果表 | 可排序, 可筛选, 可导出CSV |
| 回填按钮 | 点击最优方案 → 切换回单方案模式 → 参数自动填充 |

**左侧不变**: 依然显示基础参数（作为遍历时的"其它固定参数"）。

**改动范围**:
- `vfed-web/index.html`: 右侧模式切换Tab, 散点图容器, 结果表
- `vfed-web/main.js`: 遍历模式状态, 参数范围管理, 散点图+表格渲染
- `vfed-web/worker.js`: 已有sweep逻辑, 基本不用改

---

### Phase W3 — LED 光谱定制 UI

**依赖**: `vfed-models-plan.md` 的 M1（LED光谱模型）

**左侧新增**（在LED分组内）:

| 组件 | 内容 |
|------|------|
| 光谱预设 | 下拉: 红蓝3:1 / 红蓝4:1 / 全光谱 / 自定义 |
| 光谱分布 | 400-700nm范围, 可拖拽强度曲线(Canvas/SVG) |
| 派生值 | 自动显示: PPF(µmol/s), 真实光效(µmol/J), 推算LED功率(W) |

**右侧新增**:
- 当前光谱 vs 固定光效基准 → kWh/kg 差异百分比
- PPFD空间分布热力图(可选, 简单近似)

**改动范围**:
- `vfed-web/index.html`: LED分组展开光谱UI
- `vfed-web/main.js`: 光谱曲线交互, Worker通信
- `devices/led.py`: 光谱→光效计算（M1）

---

### Phase W5 — 全成本计算（$ / kg）

**依赖**: `vfed-models-plan.md` 的 M10（综合成本模型）

**左侧新增**（经济参数分组展开）:

| 组件 | 内容 |
|------|------|
| 设备单价 | HVAC($/W), DEH($/W), LED($/W), PV($/W), Battery($/kWh) |
| 结构成本 | 集装箱/厂房单价, 安装费比例 |
| 运营成本 | 水价($/m³), 维护率(%CAPEX/年), 人工($/年) |
| 财务参数 | 利率(%), 折旧年限, 汇率 |

**右侧新增**:
- 成本卡片: 总CAPEX, 年OPEX, $/kg, 投资回收期(年)
- 成本瀑布图: CAPEX各项 → 年OPEX各项 → $/kg

**改动范围**:
- `vfed-web/index.html`: 经济分组扩展
- `vfed-web/main.js`: 成本瀑布图(Chart.js Stacked Bar)
- `pvbes/energy_system.py`: M10经济模型扩展

---

### Phase W6 — 集装箱3D可视化

**依赖**: `vfed-models-plan.md` 的 M6+M7+M8（空间布局模型）

**右侧新增"3D视图"Tab**（与图表Tab并列）:

```
┌─────────────────────────────────┐
│ [图表面板]  [3D视图]    ← Tab   │
├─────────────────────────────────┤
│                                 │
│     Three.js / Babylon.js       │
│     可旋转/缩放/平移            │
│                                 │
│   ┌─────────────────────┐       │
│   │ 半透明集装箱外壳      │       │
│   │ ┌───┐  [过道] ┌───┐ │       │
│   │ │架 │  ≥0.6m │架 │ │       │
│   │ │1  │        │2  │ │       │
│   │ └───┘        └───┘ │       │
│   └─────────────────────┘       │
│   [外机]在外面   [水肥间]可选    │
│                                 │
│   可用栽培面积: 32.5 m²          │
│   空间利用率:   78%              │
│   日产量预估:   4.2 kg/天        │
│                                 │
└─────────────────────────────────┘
```

**3D场景元素**:

| 元素 | 交互 | 说明 |
|------|------|------|
| 集装箱外壳 | 半透明, 固定尺寸 | 外尺寸不可变, 内部净尺寸由厚度决定 |
| 围护厚度 | 左侧滑块 → 3D实时变化 | 向内增厚, 净空间缩小 |
| 栽培架 | 自动排布, 高亮 | N列 × 层数, 过道≥0.6m |
| 外机 | 固定在集装箱外 | 不可移动 |
| 水肥间/电控箱 | 可切换位置 | 内置(压缩栽培面积) / 外附(额外体积) |
| PV板 | 屋顶+立面 | 倾角/朝向可调, 实时显示覆盖面积 |
| 尺寸标注 | 自动显示 | 长/宽/高/过道宽度/架间距 |

**左侧联动**:
- 调整栽培架层数 → 3D立即新增/减少层
- 调整围护厚度 → 3D墙体向内扩张, 栽培面积卡片更新
- 调整PV面积 → 屋顶/立面光伏板面积变化

**技术选型建议**: Three.js（生态大, 文档全, 适合工程可视化）

**改动范围**:
- `vfed-web/`: 新增 `three-view.js`, 3D资源
- `src/spatial/`: 空间布局模型（M6+M7+M8）
- `vfed-web/index.html`: 右侧Tab切换, 3D画布容器

---

### Phase W7 — 自由定制空间

**目标**: 不限于集装箱, 用户自定义建筑结构。

**扩展**:
- 建筑类型选择: 集装箱 / 厂房(矩形) / 小楼宇(多层) / 自定义尺寸
- 自由拖拽栽培架位置（不再自动排布）
- 多房间/多区域: 每个区域独立设定温湿度
- PV覆盖区域交互式划定（屋顶+立面可涂选）
- 导出布局为配置（可复现）

**改动范围**:
- `vfed-web/three-view.js`: 拖拽交互, 多建筑类型
- `src/spatial/`: 扩展支持多种建筑类型

---

## 3. Cross-Cutting UX Features

### 语言切换
- 首次访问 → 弹出语言选择(中文/English, 后续可扩展)
- 顶部Bar常驻语言切换按钮
- 所有UI文本i18n化（不使用翻译API, 手动维护 `i18n/{lang}.json`）

### 无注册
- 所有状态在URL和localStorage中
- 无后端, 无数据库, 无用户表

### AI策略对比（阶段待定, 依赖M11）
- 右侧增加Tab: "AI策略对比"
- 三列对比: AI光 vs AI温 vs AI电 vs 固定基线
- 每个: kWh/kg, 年MWh, 产量, $/kg
- 柱状图对比

---

## 4. Phase Dependency Diagram

```
W1(纯数值+实时预览) ──→ W2(URL分享) ──→ W4(参数优化模式)
         │                                      │
         │                                      ├──→ W3(LED光谱UI)
         │                                      │     ↑ 依赖 M1
         │                                      │
         │                                      └──→ W5(全成本$/kg)
         │                                            ↑ 依赖 M10
         │
         └──→ W6(3D集装箱可视化)
               ↑ 依赖 M6+M7+M8+M9
               │
               └──→ W7(自由定制空间)
```

---

## 5. 不在本Plan范围的内容

以下内容归 `vfed-models-plan.md`:
- HVAC/DEH设定导向重构
- LED光谱→效率计算逻辑
- 空间布局数学模型
- 天气/电价数据库构建
- Van Henten模型改进
- AI策略算法
- 综合成本计算逻辑
- 所有Python仿真引擎改动
